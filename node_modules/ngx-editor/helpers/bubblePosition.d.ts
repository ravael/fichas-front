import { EditorView } from 'prosemirror-view';
interface TooltipPosition {
    bottom: number;
    left: number;
}
export declare const calculateBubblePos: (view: EditorView, toolTipEl: HTMLElement) => TooltipPosition;
export {};
